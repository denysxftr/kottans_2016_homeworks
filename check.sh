ruby 
  ./hw1/fibonacci_test.rb 
  && (cd hw2 && bundle install && bundle exec rspec)
  && (cd hw3/simple_router && bundle install && bundle exec rspec)
  && (cd hw4/simple_router && bundle install && bundle exec rspec)
